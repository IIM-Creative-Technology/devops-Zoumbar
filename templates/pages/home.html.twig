{% extends 'base.html.twig' %}


{% block body %}

    <img src="{{ asset('/images/30692_800.gif') }}" alt="Paris-img" class="h-64 rounded">
    <h1 class="mt-5 text-3xl sm:text-5xl semi-bold text-orange-500">Bonjour from Paris</h1>

    <p class="text-lg text-gray-800">It's currently : {{'now'|date('h:i A')}}</p>


{% endblock %}